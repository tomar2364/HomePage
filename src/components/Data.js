// import Todo from "./Todo.js";
export const Data = [
  {
    question: "",
    answer: "",
  },
];
const Doc = ({ todos }) => {
  return todos.map((todo, index) => <>Data[i].question=todo.text;</>);
};
export default Doc;
